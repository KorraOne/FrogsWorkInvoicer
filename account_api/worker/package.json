{
  "name": "frogswork-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute frogswork-account --local --file=../schema.sql"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20",
    "bcryptjs": "^2.4.3",
    "jose": "^5.9.6",
    "pdf-lib": "^1.17.1",
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
